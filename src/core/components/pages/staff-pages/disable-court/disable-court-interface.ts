export interface RowType {
    courtNum: string,
    sports: string,
    startDate: string,
    endDate: string,
    id: string
}

export interface Option {
    sportType: string[],
    courtNum: string[]
}